plugins {
    id 'java'
}

repositories {
    mavenLocal()
    maven {
        url = 'https://repo.runelite.net'
    }
    mavenCentral()
}

def runeLiteVersion = '1.12.33'

dependencies {
    compileOnly group: 'net.runelite', name: 'client', version: runeLiteVersion
    compileOnly 'org.projectlombok:lombok:1.18.30'
    annotationProcessor 'org.projectlombok:lombok:1.18.30'

    testImplementation group: 'net.runelite', name: 'client', version: runeLiteVersion
    testImplementation group: 'net.runelite', name: 'jshell', version: runeLiteVersion
}

group = 'com.osrsgo'
version = '1.0.0'
sourceCompatibility = '11'
targetCompatibility = '11'

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

// Offline battle-engine sanity check. Run: gradlew sim
task sim(type: JavaExec) {
    classpath = sourceSets.test.runtimeClasspath
    mainClass = 'com.osrsgo.SimHarness'
}

task runClient(type: JavaExec) {
    classpath = sourceSets.test.runtimeClasspath
    mainClass = 'com.osrsgo.OsrsGoPluginTest'
    jvmArgs = [
        '-ea',
        '-Drunelite.insecure-skip-tls-verification=true'
    ]
    args = ['--insecure-skip-tls-verification', '--developer-mode']
}
